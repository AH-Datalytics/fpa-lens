name: Twice-weekly data refresh

# Pulls the newest source files from the SharePoint LensRepository, regenerates
# the dashboard JSON in public/data/, commits whatever changed (which triggers a
# Vercel deploy), and emails the data-ops contact when data publishes or a
# category fails. Runs Tuesday and Friday mornings; can also be triggered
# manually. Fully hands-off otherwise.

on:
  schedule:
    - cron: "0 13 * * 2,5" # Tue + Fri ~07:00-08:00 America/Chicago (UTC, no DST shift)
  workflow_dispatch: {}

permissions:
  contents: write

concurrency:
  group: data-refresh
  cancel-in-progress: false

jobs:
  refresh:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v6
        with:
          node-version: "20"

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install Python deps
        run: pip install openpyxl

      - name: Refresh data from SharePoint
        id: refresh
        env:
          SHAREPOINT_TENANT_ID: ${{ secrets.SHAREPOINT_TENANT_ID }}
          SHAREPOINT_CLIENT_ID: ${{ secrets.SHAREPOINT_CLIENT_ID }}
          SHAREPOINT_CLIENT_SECRET: ${{ secrets.SHAREPOINT_CLIENT_SECRET }}
          SHAREPOINT_HOST: ${{ secrets.SHAREPOINT_HOST }}
          SHAREPOINT_SITE: ${{ secrets.SHAREPOINT_SITE }}
          SHAREPOINT_LIBRARY: ${{ secrets.SHAREPOINT_LIBRARY }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: node scripts/refresh-data.mjs

      # Email a digest of the run: what data changed, per-source pull status, and
      # any failures. Runs before the commit step so it can diff the working tree
      # against HEAD to describe the changes. The script stays silent on runs
      # with nothing to report; a manual run always
      # emails (FORCE_DIGEST) so you get confirmation you asked for. Never fails
      # the run (always exits 0).
      - name: Email refresh digest
        if: always()
        env:
          RESEND_API_KEY: ${{ secrets.RESEND_API_KEY }}
          RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
          REFRESH_OUTCOME: ${{ steps.refresh.outcome }}
          FORCE_DIGEST: ${{ github.event_name == 'workflow_dispatch' && '1' || '0' }}
        run: node scripts/notify-digest.mjs

      # Commit even on partial failure so categories that DID update still publish.
      - name: Commit refreshed data
        if: always()
        run: |
          git config user.name "fpa-lens-bot"
          git config user.email "oboochever@ahdatalytics.com"
          git add public/data src/data/turfCycles.json
          if git diff --staged --quiet; then
            echo "No data changes this run."
          else
            git commit -m "chore: data refresh [skip ci]"
            git push
          fi
